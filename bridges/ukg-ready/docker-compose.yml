# Generated by trellis -- regenerated on each build
# Bridge: ukg-ready v2026.02.27.2
services:
  ukg-ready:
    image: bridge-ukg-ready:latest
    build: .
    container_name: bridge-ukg-ready
    hostname: bridge-ukg-ready
    restart: unless-stopped
    volumes:
      - ukg-ready-tendril:/opt/tendril
      - ukg-ready-data:/opt/bridge/data
    environment:
      - TZ=UTC
      - TENDRIL_INSTALL_KEY=${TENDRIL_INSTALL_KEY}
      - TENDRIL_DOWNLOAD_URL=${TENDRIL_DOWNLOAD_URL}
      - UKG_BASE_URL=${UKG_BASE_URL}
      - UKG_COMPANY_SHORT_NAME=${UKG_COMPANY_SHORT_NAME}
      - UKG_API_KEY=${UKG_API_KEY}
      - UKG_USERNAME=${UKG_USERNAME}
      - UKG_PASSWORD=${UKG_PASSWORD}
      - UKG_VERIFY_TLS=${UKG_VERIFY_TLS:-true}
    healthcheck:
      test: ["CMD-SHELL", "python3 /opt/bridge/data/tools/ukg_check.py || exit 1"]
      interval: 60s
      timeout: 15s
      retries: 3

volumes:
  ukg-ready-tendril:
  ukg-ready-data:
