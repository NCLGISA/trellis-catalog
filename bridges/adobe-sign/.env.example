# ============================================================================
# Tendril Bridge - Adobe Sign - Environment Variables
# ============================================================================
# Copy this to .env and fill in your values.
# docker-compose reads .env automatically from the same directory.
#
# NEVER commit .env files with real credentials to version control.
# Generated from bridge.yaml -- regenerate with: trellis build adobe-sign
# ============================================================================

# === Tendril Agent Credentials ===
TENDRIL_INSTALL_KEY=
TENDRIL_DOWNLOAD_URL=https://tendril.example.com/download/tendril?linux-amd64

# === Adobe Sign Integration Key ===
# Generate from: Adobe Sign > Account > Personal Preferences > Access Tokens
# Create an Integration Key with scopes: account_read, user_read,
# agreement_read, agreement_write, agreement_send, widget_read,
# library_read, workflow_read, webhook_read, webhook_write
ADOBE_SIGN_INTEGRATION_KEY=
