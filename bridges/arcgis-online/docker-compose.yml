# Generated by trellis -- regenerated on each build
# Bridge: arcgis-online v2026.03.06.1
services:
  arcgis-online:
    image: bridge-arcgis-online:latest
    build: .
    container_name: bridge-arcgis-online
    hostname: bridge-arcgis-online
    restart: unless-stopped
    volumes:
      - bridge-arcgis-online-tendril:/opt/tendril
      - bridge-arcgis-online-data:/opt/bridge/data
    environment:
      - TZ=${TZ:-UTC}
      - TENDRIL_INSTALL_KEY=${TENDRIL_INSTALL_KEY}
      - TENDRIL_DOWNLOAD_URL=${TENDRIL_DOWNLOAD_URL}
      - ARCGIS_ORG_URL=${ARCGIS_ORG_URL}
      - ARCGIS_CLIENT_ID=${ARCGIS_CLIENT_ID}
      - ARCGIS_CLIENT_SECRET=${ARCGIS_CLIENT_SECRET}
    healthcheck:
      test: ["CMD-SHELL", "python3 /opt/bridge/data/tools/arcgis_online_check.py --quick || exit 1"]
      interval: 120s
      timeout: 15s
      retries: 3

volumes:
  bridge-arcgis-online-tendril:
  bridge-arcgis-online-data:
