# Generated by trellis -- regenerated on each build
# Bridge: sophos-central v2026.02.24.1
services:
  sophos:
    image: bridge-sophos:latest
    build: .
    container_name: bridge-sophos
    hostname: bridge-sophos
    restart: unless-stopped
    volumes:
      - sophos-tendril:/opt/tendril
      - sophos-data:/opt/bridge/data
    environment:
      - TZ=UTC
      - TENDRIL_INSTALL_KEY=${TENDRIL_INSTALL_KEY}
      - TENDRIL_DOWNLOAD_URL=${TENDRIL_DOWNLOAD_URL}
      - SOPHOS_CLIENT_ID=${SOPHOS_CLIENT_ID}
      - SOPHOS_CLIENT_SECRET=${SOPHOS_CLIENT_SECRET}
    healthcheck:
      test: ["CMD-SHELL", "python3 /opt/bridge/data/tools/sophos_check.py || exit 1"]
      interval: 60s
      timeout: 15s
      retries: 3

volumes:
  sophos-tendril:
  sophos-data:
