# Generated by trellis -- regenerated on each build
# Bridge: hycu v2026.02.26.1
services:
  hycu:
    image: bridge-hycu:latest
    build: .
    container_name: bridge-hycu
    hostname: bridge-hycu
    restart: unless-stopped
    volumes:
      - hycu-tendril:/opt/tendril
      - hycu-data:/opt/bridge/data
    environment:
      - TZ=UTC
      - TENDRIL_INSTALL_KEY=${TENDRIL_INSTALL_KEY}
      - TENDRIL_DOWNLOAD_URL=${TENDRIL_DOWNLOAD_URL}
    healthcheck:
      test: ["CMD-SHELL", "python3 /opt/bridge/data/tools/hycu_check.py || exit 1"]
      interval: 60s
      timeout: 30s
      retries: 3

volumes:
  hycu-tendril:
  hycu-data:
