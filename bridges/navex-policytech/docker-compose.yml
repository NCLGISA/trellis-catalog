# Generated by trellis -- regenerated on each build
# Bridge: navex-policytech v2026.02.27.2
services:
  navex-policytech:
    image: bridge-navex-policytech:latest
    build: .
    container_name: bridge-navex-policytech
    hostname: bridge-navex-policytech
    restart: unless-stopped
    volumes:
      - navex-policytech-tendril:/opt/tendril
      - navex-policytech-data:/opt/bridge/data
    environment:
      - TZ=UTC
      - TENDRIL_INSTALL_KEY=${TENDRIL_INSTALL_KEY}
      - TENDRIL_DOWNLOAD_URL=${TENDRIL_DOWNLOAD_URL}
      - POLICYTECH_BASE_URL=${POLICYTECH_BASE_URL}
      - POLICYTECH_API_KEY=${POLICYTECH_API_KEY}
      - POLICYTECH_VERIFY_TLS=${POLICYTECH_VERIFY_TLS:-true}
    healthcheck:
      test: ["CMD-SHELL", "python3 /opt/bridge/data/tools/policytech_check.py || exit 1"]
      interval: 60s
      timeout: 15s
      retries: 3

volumes:
  navex-policytech-tendril:
  navex-policytech-data:
