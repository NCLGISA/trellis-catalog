# Munis ODBC bridge environment variables

# Tendril agent registration key (from your Tendril Root admin panel)
TENDRIL_INSTALL_KEY=

# Tendril agent download URL
TENDRIL_DOWNLOAD_URL=https://your-tendril-root/download/tendril?linux-amd64

# Munis SQL Server hostname (reachable via site-to-site VPN to Tyler cloud)
# Find this in Tyler Deploy: Site Report > Environment > Munis ERP > Database Info > SQL Instance
# Append .tylerhost.net to the SQL Instance value
MUNIS_DB_HOST=your-sql-instance.tylerhost.net

# Munis database name
# Find this in Tyler Deploy: Site Report > Environment > Munis ERP > Database Info > Database User
MUNIS_DB_NAME=your-database-name

# Per-operator credentials are managed via the Tendril Root credential vault.
# MUNIS_DB_USER and MUNIS_DB_PASSWORD are injected at runtime per-operator
# and do not need to be set here.
