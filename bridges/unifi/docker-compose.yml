# Generated by trellis -- regenerated on each build
# Bridge: unifi v2026.03.03.1
services:
  unifi:
    image: bridge-unifi:latest
    build: .
    container_name: bridge-unifi
    hostname: bridge-unifi
    restart: unless-stopped
    volumes:
      - bridge-unifi-tendril:/opt/tendril
      - bridge-unifi-data:/opt/bridge/data
    environment:
      - TZ=America/New_York
      - TENDRIL_INSTALL_KEY=${TENDRIL_INSTALL_KEY}
      - TENDRIL_DOWNLOAD_URL=${TENDRIL_DOWNLOAD_URL}
      - UNIFI_URL=${UNIFI_URL}
      - UNIFI_USERNAME=${UNIFI_USERNAME}
      - UNIFI_PASSWORD=${UNIFI_PASSWORD}
      - UNIFI_SITE=${UNIFI_SITE}
    healthcheck:
      test: ["CMD-SHELL", "python3 /opt/bridge/data/tools/unifi_check.py --quick --quick || exit 1"]
      interval: 60s
      timeout: 10s
      retries: 3

volumes:
  bridge-unifi-tendril:
  bridge-unifi-data:
