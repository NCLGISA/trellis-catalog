# Generated by trellis -- regenerated on each build
# Bridge: splunk v2026.02.24.2
services:
  splunk:
    image: bridge-splunk:latest
    build: .
    container_name: bridge-splunk
    hostname: bridge-splunk
    restart: unless-stopped
    volumes:
      - splunk-tendril:/opt/tendril
      - splunk-data:/opt/bridge/data
    environment:
      - TZ=UTC
      - TENDRIL_INSTALL_KEY=${TENDRIL_INSTALL_KEY}
      - TENDRIL_DOWNLOAD_URL=${TENDRIL_DOWNLOAD_URL}
      - SPLUNK_URL=${SPLUNK_URL}
      - SPLUNK_VERIFY_TLS=${SPLUNK_VERIFY_TLS:-true}
    healthcheck:
      test: ["CMD-SHELL", "python3 /opt/bridge/data/tools/splunk_check.py || exit 1"]
      interval: 60s
      timeout: 15s
      retries: 3

volumes:
  splunk-tendril:
  splunk-data:
