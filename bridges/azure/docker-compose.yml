# Generated by trellis -- regenerated on each build
# Bridge: azure v2026.02.19.1
services:
  azure:
    image: bridge-azure:latest
    build: .
    container_name: bridge-azure
    hostname: bridge-azure
    restart: unless-stopped
    volumes:
      - azure-tendril:/opt/tendril
      - azure-data:/opt/bridge/data
    environment:
      - TZ=UTC
      - TENDRIL_INSTALL_KEY=${TENDRIL_INSTALL_KEY}
      - TENDRIL_DOWNLOAD_URL=${TENDRIL_DOWNLOAD_URL}
      - AZURE_CLOUD=${AZURE_CLOUD}
      - AZURE_TENANT_ID=${AZURE_TENANT_ID}
      - ARM_CLIENT_ID=${ARM_CLIENT_ID}
      - ARM_CLIENT_SECRET=${ARM_CLIENT_SECRET}
      - AZURE_SUBSCRIPTION_ID=${AZURE_SUBSCRIPTION_ID}
    healthcheck:
      test: ["CMD-SHELL", "python3 /opt/bridge/data/tools/arm_check.py || exit 1"]
      interval: 60s
      timeout: 30s
      retries: 3

volumes:
  azure-tendril:
  azure-data:
